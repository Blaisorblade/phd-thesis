\documentclass{../sig-alternate}
\usepackage[margin=0cm,a5paper,landscape]{geometry}
\usepackage{fontspec}
\setromanfont{Optima}
\input{../macros}

\newcommand{\graphPath}[1]{#1}
\begin{document}

\newcommand{\tablerowsize}{\small}
\newcommand{\captionEvalTable}{
Performance results.
Implementations:
(1) modular;
(2) hand-optimized;
(3) with SQuOpt:
($3^-$) without optimization;\\
($3^o$) with optimization;
($3^x$) with indexes.
Queries marked with $^R$ were selected by random sampling.
\pg{Extend caption. Reconsider which columns to show. H is important to
acknowledge our limitations (if we want). Resort queries.}
}

\begin{table*}[tb]
%\begin{center}
\input{\graphPath{EvalTable}}
%\end{center}
%\nocaptionrule\caption{Performance results.}
%\label{table:performance}
\end{table*}
\end{document}
